faster-whisper
numpy
soundfile
# Install a torch wheel appropriate for your machine (see https://pytorch.org)
# Example (Linux + CUDA 11.8): pip install torch --index-url https://download.pytorch.org/whl/cu118
